---
import { site } from '../data/site';
import { picsum } from '../data/photos';
import { theme } from '../data/themes';
---

<section class="hero hero--centered" style={`--hero-image: url('${picsum(theme.heroPhoto.id, 1920, 1080)}')`}>
  <div>
    <h1 class="hero-title fadeup">Welcome!</h1>
    <p class="hero-tagline fadeup">{site.tagline}</p>
    <div class="hero-actions fadeup">
      <a class="button" href="/gallery">To gallery</a>
    </div>
  </div>
</section>
